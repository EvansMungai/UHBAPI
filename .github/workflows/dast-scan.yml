name: DAST Security Scan

on:
  workflow_run:
    workflows: ["Secure CI Pipeline (SAST & SCA)"]
    types:
      - completed

jobs:
  zap-dast:
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.conclusion == 'success' && 
      github.event.workflow_run.event != 'pull_request'

    permissions:
      contents: read
      actions: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Start application
        run: |
          docker-compose up -d uhb
          sleep 5

      - name: Run ZAP API Scan Action
        uses: zaproxy/action-api-scan@5158fe4740e53a2efec746bf986a427f714902ff
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          token: $GITHUB_TOKEN
          format: openapi
          target: 'http://UHB:8080/swagger/v1/swagger.json'
          rules_file_name: 'zap.conf'
          cmd_options: '-r api-report.html'
          docker_name: 'ghcr.io/zaproxy/zaproxy:stable'

      - name: Stop Containers
        if: always()
        run: docker compose down

      - name: Upload Security Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-dast-report
          path: |
            api-report.html